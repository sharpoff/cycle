#pragma once

#include <stdint.h>

enum class EntityID : uint32_t { Invalid = UINT32_MAX };
enum class MeshID : uint32_t { Invalid = UINT32_MAX };
enum class TextureID : uint32_t { Invalid = UINT32_MAX };
enum class MaterialID : uint32_t { Invalid = UINT32_MAX };

enum class RenderPassID : uint32_t { Invalid = UINT32_MAX };